// src/pages/auto/theme.js
//
// Trimora Auto's token system -- deliberately not POS's gold/black salon
// palette. The kickoff brief is explicit: "Not a traditional POS
// look-and-feel... fast, modern, premium, touch-friendly, mobile-first."
// This is a staff tool used standing up at a bay, often on a tablet,
// moving fast between vehicles -- so the palette leans cool/technical
// (steel, chrome, a clean signal-green) rather than the salon's warm
// gold, and the one signature element (see CheckInPage's plate input)
// is grounded in the actual subject: a vehicle registration plate.

export var INK    = "#0B1220"; // page background
export var STEEL  = "#16202E"; // panel background
export var CHROME = "#8FA6B8"; // secondary text, borders
export var SIGNAL = "#3DDC97"; // primary action / "ready" accent
export var ALERT  = "#FF6B4A"; // errors, destructive actions
export var PAPER  = "#F5F7FA"; // high-contrast text on dark
